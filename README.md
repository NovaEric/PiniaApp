# PiniaApp Demos

## Dev
Steps

1. clone repo
2. run:

    ``` yarn install ```
    or
    ``` npm install ```

3. run backend server

    ``` yarn backend-server ```
    or 
    ``` npm run backend-server ```

4. run client

     ``` yarn dev ```
    or 
    ``` npm run dev ```